<?php require_once 'layout/header.phtml'; ?>

<div class="edit-category-wrapper"> 
    <form action="<?= BASE_URL ?>editar-categoria/<?= $category->id ?>" method="POST" class="edit-form">
        <h2>Editar Categoría: <?= $category->name ?></h2>

        <input type="hidden" name="id" value="<?= $category->id ?>">

        <div class="form-group">
            <label for="name">Nombre:</label>
            <input type="text" name="name" id="name" value="<?= $category->name ?>" required>
        </div>

        <div class="form-group">
            <label for="img">URL de Imagen:</label>
            <input type="url" name="img" id="img" value="<?= $category->img ?>" required>
        </div>

        <div class="form-group">
            <label for="description">Descripción:</label>
            <textarea name="description" id="description" required><?= $category->description ?></textarea>
        </div>

        <button type="submit">Guardar Cambios</button>
        <a href="<?= BASE_URL ?>categorias" class="btn-cancel">Cancelar</a>
    </form>
</div>

<?php require_once 'layout/footer.phtml'; ?>